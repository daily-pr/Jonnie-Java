@Enumerated 어노테이션 관련하여

@Enumerated의 어노테이션에는 두가지 EnumType이 존재함.
타입을 지정하지 않고 단순히 enum을 사용하게 되면 DB에 값은 정수형으로 저장.

주의사항 : enum 값(name)으로DB에 젖아하면 더 알아보기 쉽지만, 하지만 enum 값을 저장하는 과정에서 int가 string을 DB에 저장하게 되면 메모리 용량을 좀 더 증가시킬 수 있다. 
